# Examples

This directory contains non-authoritative examples for inspection.

- `generated/person.sql` is a previously generated SQL artifact.

Examples are not loaded by the agent. Active inputs belong in `specs/`, and
new local generation outputs belong in `output/`.
