# Secure run logs

Local CLI generation writes owner-only JSON run logs into this directory.

Run logs and provider transcripts are intentionally ignored by Git. Review
them before sharing because they can contain source metadata, mapping comments
and provider diagnostics.
